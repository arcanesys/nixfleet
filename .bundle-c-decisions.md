# Bundle C — locked decisions (D12-D18)

Working notes for the TPM-bound issuance CA bundle. NOT for commit.

| # | Decision | Locked value | Notes |
|---|---|---|---|
| D12 | Fleet root CA storage | File-based v1; Yubikey path supported in bootstrap from day 1 | Operator will purchase Yubikey; swap is a config flip |
| D13 | TPM library on CP | Shell-out via `tpm-sign` wrapper | Reuses existing keyslots scope substrate; ~0 new Rust LOC vs ~400 for tss-esapi |
| D14 | Issuance CA name constraints | `dNSName: *.fleet.lab.internal` + `EKU: clientAuth only` | CN format: `agent-<machineId>.fleet.lab.internal`. 5-cert migration via existing `/renew` |
| D15 | Issuance CA validity | 1 year | Annual rotation via Bundle B trust-rotation primitive |
| D16 | PCR policy | **PCR 0 only** (UEFI firmware) for C.1 | Lab has Secure Boot disabled (PCR 7 unmeasured). PCR 0 stable across kernel/bootloader updates. Follow-up tracked at #83 (PCR expansion); prerequisite tracked at #84 (Secure Boot via lanzaboote) |
| D17 | Bootstrap UX | `nixfleet-cp-bootstrap` on operator workstation, drives lab via SSH | `--dry-run` + confirmation prompts |
| D18 | Audit log | Keep current `/var/lib/nixfleet-cp/issuance.log` (file + journal) | Defer signed shipping |

## Phasing

| Phase | Deliverable | Branch |
|---|---|---|
| C.1 | Multi-keyslot substrate (attrset), PCR 7 policy, framework default-on/opt-out toggle | `feat/41-tpm-bound-issuance-ca` (this worktree) |
| C.2 | `CaSigner` trait + `TpmCaSigner` (shell-out via `tpm-sign`); CP runs on TPM-bound issuance CA when flagged | TBD |
| C.3 | Name constraints + cert template; trust.json schema for root/issuance chain | TBD |
| C.4 | Operator bootstrap script (file root + Yubikey paths) | TBD |
| C.5 | Live migration on lab using Bundle B trust rotation | TBD |
